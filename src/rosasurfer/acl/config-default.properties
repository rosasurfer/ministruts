
# ACL default configuration (not yet implemented)
acl.config.defaultMode          = Deny                                        # Allow|Deny   (default: Deny)
acl.config.subject.roles        = On                                          # On|Off       (default: On)
acl.config.subject.policies     = On                                          # On|Off       (default: On)
acl.config.adapter.class        = rosasurfer\acl\adapter\ConfigFileAdapter    # {classname}  (default: rosasurfer\acl\adapter\ConfigFileAdapter)
acl.config.adapter.options.file =                                             # {filename}   (default: application configuration)
acl.config.role.class           = rosasurfer\acl\Role                         # {classname}  (default: rosasurfer\acl\Role)
acl.config.policy.class         = rosasurfer\acl\Policy                       # {classname}  (default: rosasurfer\acl\Policy)
